let customerName: string = "John";
let customerLastName: string = "Doe";
let customerAge: number = 30;
let isCustomer: boolean = true;

// Custom types
type Customer = { name: string, lastName: string, age: number, isCustomer: boolean }

let customer: Customer = {
    name: "John",
    lastName: "Doe",
    age: 30,
    isCustomer: true
}

